<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_move_line_form_inherit_inherit" model="ir.ui.view">
        <field name="name">stock.move.line.form.inherit.inherit</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_move_line_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='qty_done']" position="after">
                <!-- <field name="cur_location_stock_quant" optional="hide"/>
                <field name="cur_location_dest_stock_quant" optional="hide"/> -->
            </xpath>
        </field>
    </record>
</odoo>
