<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_stock_quant_form_inherit_inherit" model="ir.ui.view">
        <field name="name">stock.quant.form.inherit.inherit</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree_inventory_editable"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='inventory_diff_quantity']" position="after">
                <!-- <button name="action_reset_quantity" type="object" string="Cập nhật" class="btn btn-link" icon="fa-refresh" attrs="{'invisible': [('inventory_quantity_set', '=', False)]}"/> -->
                <field name="reserved_quantity" optional="hide"/>
            </xpath>
        </field>
    </record>
</odoo>
