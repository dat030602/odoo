<odoo>
    <record id="view_create_picking_wizard" model="ir.ui.view">
        <field name="name">create.picking.wizard.form</field>
        <field name="model">create.picking.wizard</field>
        <field name="arch" type="xml">
            <form string="Tạo phiếu xuất kho">
                <group>
                    <field name="approval_request_id" readonly="1" invisible="1"/>
                    <field name="picking_id" readonly="1" invisible="1"/>
                    <field name="partner_id" options="{'no_open': True, 'no_create': True}"/>
                    <field name="scheduled_date"/>
                </group>
                <notebook>
                    <page string="Chi tiết" name="line_ids">
                        <field name="line_ids">
                            <tree editable='bottom'>
                                <field name="picking_type_id"/>
                                <field name="partner_id" options="{'no_open': True, 'no_create': True}"/>
                                <field name="product_id"/>
                                <field name="quantity"/>
                                <field name="product_uom"/>
                            </tree>
                        </field>
                    </page>
                </notebook>
                <footer>
                    <button string="Tạo phiếu" type="object" name="action_create_picking" class="btn-primary"/>
                    <button string="Hủy" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <act_window id="action_create_picking_wizard"
        name="Tạo phiếu kho"
        res_model="create.picking.wizard"
        view_mode="form"
        target="new"
        binding_model="approval.request"
    />
</odoo>
