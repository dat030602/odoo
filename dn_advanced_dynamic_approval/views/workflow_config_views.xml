<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="approval_workflow_config_view_tree" model="ir.ui.view">
        <field name="name">approval.workflow.config.list</field>
        <field name="model">approval.workflow.config</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="category_id"/>
                <field name="model_id"/>
                <field name="technical_name"/>
                <field name="state" widget="badge" decoration-info="state == 'draft'" decoration-success="state == 'locked'"/>
            </list>
        </field>
    </record>

    <record id="approval_workflow_config_view_form" model="ir.ui.view">
        <field name="name">approval.workflow.config.form</field>
        <field name="model">approval.workflow.config</field>
        <field name="arch" type="xml">
            <form string="Workflow Configuration">
                <header>
                    <button name="action_generate_views" string="Generate Views" type="object" class="btn-primary" invisible="action_id or menu_id or list_view_id or form_view_id"/>
                    <button name="action_create_server_action" string="Create Server Action" type="object" class="btn-secondary" invisible="server_id"/>
                    <field name="state" widget="statusbar" options="{'clickable': '1'}"/>
                </header>
                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_open_stages" string="Stages" type="object" class="oe_stat_button" icon="fa-list" invisible="stage_ids == []"/>
                        <button name="action_open_server_action" string="Server Action" type="object" class="oe_stat_button" icon="fa-cogs" invisible="server_id == False"/>
                    </div>
                    <div class="d-flex justify-content-between align-items-center">
                        <h1 class="flex-grow-1">
                            <field class="text-break" options="{'line_breaks': False}" widget="text" name="name" placeholder="e.g. Approval Workflow" readonly="state == 'locked'"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="technical_name" readonly="state == 'locked'"/>
                        </group>
                        <group>
                            <field name="category_id" readonly="state == 'locked'"/>
                            <field name="model_id" readonly="state == 'locked'"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Code">
                            <field name="init_code" widget="code" options="{'mode': 'python'}" readonly="state == 'locked'"/>
                        </page>
                        <page string="Views">
                            <group>
                                <field name="action_id"/>
                                <field name="menu_id"/>
                                <field name="list_view_id"/>
                                <field name="form_view_id"/>
                            </group>
                        </page>
                        <page string="Help" name="code_help">
                            <p>The following variables can be used:</p>
                            <ul>
                                <li><code>env</code>: environment on which the action is triggered</li>
                                <li><code>model</code>: model of the record on which the action is triggered; is a void recordset</li>
                                <li><code>record</code>: record on which the action is triggered; may be be void</li>
                                <li><code>records</code>: recordset of all records on which the action is triggered in multi mode; may be void</li>
                                <li><code>time</code>, <code>datetime</code>, <code>dateutil</code>, <code>timezone</code>: useful Python libraries</li>
                                <li><code>float_compare()</code>: utility function to compare floats based on a specific precision</li>
                                <li><code>log(message, level='info')</code>: logging function to record debug information in <code>ir.logging</code> table</li>
                                <li><code>_logger.info(message)</code>: logger to emit messages in server logs</li>
                                <li><code>UserError</code>: exception class for raising user-facing warning messages</li>
                                <li><code>Command</code>: x2many commands namespace</li>
                                <li>To return an action, assign: <code>action = {...}</code></li>
                                <li>To notify progress for CRON call and re-trigger a call if there is remaining tasks, use <code>env['ir.cron']._notify_progress(done=task_done_count, remaining=task_remaining_count)</code></li>
                            </ul>
                            <p>
                                <widget name="documentation_link"
                                        path="/developer/reference/backend/orm.html"
                                        label="ORM API Documentation"
                                        icon="oi oi-fw oi-arrow-right me-1"/>
                            </p>
                            <div >
                                <p>Example of Python code:</p>
                                <pre style='white-space: pre-wrap'>partner_name = record.name + '_code'
env['res.partner'].create({'name': partner_name})
</pre>
                            </div>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="approval_workflow_config_action" model="ir.actions.act_window">
        <field name="name">Workflow Config</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">approval.workflow.config</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first workflow configuration
            </p>
        </field>
    </record>

    <menuitem id="approval_workflow_config_menu"
              name="Workflow"
              parent="approvals.approvals_menu_config"
              action="approval_workflow_config_action"
              sequence="10"/>
</odoo>
