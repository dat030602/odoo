<odoo>
    <record id="view_sale_order_detail_wizard_form" model="ir.ui.view">
        <field name="name">sale.order.detail.wizard.form</field>
        <field name="model">sale.order.detail.wizard</field>
        <field name="arch" type="xml">
            <form string="Chi tiết đơn bán hàng" readonly="1">
                <group>
                    <field name="order_id"/>
                    <field name="partner_id" options="{'no_open': True, 'no_create': True}"/>
                    <field name="date_order"/>
                    <field name="state"/>
                </group>
                <group>
                    <field name="order_line_ids" readonly="1">
                        <tree>
                            <field name="product_id"/>
                            <field name="product_uom_qty"/>
                            <field name="qty_delivered"/>
                            <field name="qty_invoiced"/>
                            <field name="price_unit"/>
                            <field name="tax_id" widget="many2many_tags"/>
                            <field name="price_subtotal"/>
                        </tree>
                    </field>
                </group>
                <footer>
                    <button string="Đóng" class="btn-primary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo> 