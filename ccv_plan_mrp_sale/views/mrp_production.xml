<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- model.name inherit form view -->
    <record id="mrp_production_inherit_view_form" model="ir.ui.view">
        <field name="name">mrp.production.inherit.view.form</field>
        <field name="model">mrp.production</field>
        <field name="inherit_id" ref="mrp.mrp_production_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='miscellaneous']//field[@name='date_deadline']/.." position="inside">
                <field name="sale_ids" widget="many2many_tags"/>
                <field name="partner_ids" widget="many2many_tags"/>
            </xpath>
        </field>
    </record>
</odoo>
