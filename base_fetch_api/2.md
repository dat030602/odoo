# Universal REST Connector Framework — Tài liệu Dev

*Chi tiết triển khai kỹ thuật, dựa trên bản thiết kế đã chốt.*

---

## 0. Cấu trúc module

```
connector_base/
├── __manifest__.py
├── models/
│   ├── connector_config.py          # Config kết nối (base_url, auth...)
│   ├── connector_auth.py            # Abstract auth strategies
│   ├── connector_endpoint.py        # Định nghĩa endpoint + prepare request code
│   ├── connector_pagination.py      # Pagination strategy
│   ├── connector_model_signature.py # Discovery / schema signature
│   ├── connector_code_mixin.py      # Mixin thực thi code chung
│   ├── connector_code_execution_log.py
│   ├── connector_schema_builder.py  # Service tạo ir.model/ir.model.fields/view
│   ├── connector_import_engine.py   # Service import raw data
│   ├── connector_sync_log.py        # Log sync run
│   └── connector_missing_log.py     # Log field thiếu khi Auto Schema OFF
├── views/
├── security/
│   ├── ir.model.access.csv
│   └── security_groups.xml
└── data/
    └── ir_cron_data.xml
```

---

## 1. Model: `connector.config`

```python
class ConnectorConfig(models.Model):
    _name = "connector.config"
    _description = "Cấu hình kết nối tới 1 REST API"

    name = fields.Char(required=True)
    base_url = fields.Char(required=True)
    auth_type = fields.Selection([
        ("none", "None"),
        ("bearer", "Bearer Token"),
        ("basic", "Basic Auth"),
        ("api_key", "API Key"),
        ("oauth2", "OAuth2"),
        ("custom", "Custom (code)"),
    ], default="none", required=True)

    # Bearer / API Key
    token = fields.Char(groups="base.group_system")
    api_key_header = fields.Char(default="X-API-Key")

    # Basic
    username = fields.Char()
    password = fields.Char(groups="base.group_system")

    # OAuth2
    oauth_client_id = fields.Char()
    oauth_client_secret = fields.Char(groups="base.group_system")
    oauth_token_url = fields.Char()
    oauth_access_token = fields.Char(groups="base.group_system")
    oauth_refresh_token = fields.Char(groups="base.group_system")
    oauth_expires_at = fields.Datetime()

    default_headers = fields.Text(help="JSON dict, vd {\"Content-Type\": \"application/json\"}")
    endpoint_ids = fields.One2many("connector.endpoint", "connector_id")
    auto_create_schema = fields.Boolean(default=True)
    active = fields.Boolean(default=True)
```

### Auth resolver

```python
    def _get_auth_headers(self):
        """Trả về dict headers auth, tự refresh token OAuth2 nếu hết hạn."""
        self.ensure_one()
        headers = {}
        if self.auth_type == "bearer":
            headers["Authorization"] = f"Bearer {self.token}"
        elif self.auth_type == "basic":
            import base64
            raw = f"{self.username}:{self.password}".encode()
            headers["Authorization"] = f"Basic {base64.b64encode(raw).decode()}"
        elif self.auth_type == "api_key":
            headers[self.api_key_header] = self.token
        elif self.auth_type == "oauth2":
            if not self.oauth_access_token or (
                self.oauth_expires_at and self.oauth_expires_at <= fields.Datetime.now()
            ):
                self._oauth2_refresh()
            headers["Authorization"] = f"Bearer {self.oauth_access_token}"
        return headers

    def _oauth2_refresh(self):
        self.ensure_one()
        resp = requests.post(self.oauth_token_url, data={
            "grant_type": "refresh_token",
            "refresh_token": self.oauth_refresh_token,
            "client_id": self.oauth_client_id,
            "client_secret": self.oauth_client_secret,
        }, timeout=30)
        resp.raise_for_status()
        data = resp.json()
        self.sudo().write({
            "oauth_access_token": data["access_token"],
            "oauth_refresh_token": data.get("refresh_token", self.oauth_refresh_token),
            "oauth_expires_at": fields.Datetime.now() + timedelta(seconds=data.get("expires_in", 3600)),
        })
```

---

## 2. Model: `connector.endpoint`

```python
class ConnectorEndpoint(models.Model, ConnectorCodeMixin):
    _name = "connector.endpoint"
    _inherit = ["connector.code.mixin"]
    _description = "1 endpoint cụ thể (vd: GET /orders)"

    connector_id = fields.Many2one("connector.config", required=True, ondelete="cascade")
    name = fields.Char(required=True)
    method = fields.Selection([
        ("GET", "GET"), ("POST", "POST"), ("PUT", "PUT"),
        ("PATCH", "PATCH"), ("DELETE", "DELETE"),
    ], default="GET", required=True)
    path = fields.Char(required=True, help="vd: /admin/api/2026-01/orders.json")
    target_json_path = fields.Char(help="vd: result.data hoặc orders")
    root_model_id = fields.Many2one("connector.model.signature")

    # Pagination
    pagination_type = fields.Selection([
        ("none", "None"), ("page", "Page number"),
        ("offset", "Offset/Limit"), ("cursor", "Cursor/Next token"),
    ], default="none")
    pagination_param_page = fields.Char(default="page")
    pagination_param_limit = fields.Char(default="limit")
    pagination_page_size = fields.Integer(default=100)
    pagination_next_path = fields.Char(help="JSON path chứa next cursor/token, vd: meta.next_cursor")

    # Prepare request code (dùng connector.code.mixin -> field `code`)
    use_custom_prepare_request = fields.Boolean(
        help="Nếu bật, dùng `code` để build query params/body/headers thay vì config tĩnh."
    )
```

### Button gọi từ view (context có `active_model`, `active_ids`)

```python
    def button_execute_from_record(self):
        """Gọi từ 1 nút trên form view của model business (vd sale.order),
        dùng record hiện tại để build request và gửi lên API ngoài."""
        self.ensure_one()
        active_model = self.env.context.get("active_model")
        active_ids = self.env.context.get("active_ids", [])
        records = self.env[active_model].browse(active_ids) if active_model else self.env["base"]

        if self.use_custom_prepare_request:
            payload = self.execute_code(extra_context={"records": records})
        else:
            payload = self._prepare_request_static(records)

        return self.connector_id._request(
            method=self.method,
            path=self.path,
            json_body=payload,
        )
```

---

## 3. Code Execution Mixin — chi tiết đầy đủ

```python
# connector_code_mixin.py
import logging
import datetime
import dateutil
import json
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError
from odoo.tools.safe_eval import safe_eval

_logger = logging.getLogger(__name__)


class ConnectorCodeMixin(models.AbstractModel):
    _name = "connector.code.mixin"
    _description = "Mixin thực thi Python code an toàn (dùng cho Signature & Prepare Request)"

    code = fields.Text(
        string="Python Code",
        groups="connector_base.group_connector_developer",
        help=(
            "Biến có sẵn: env, record, records, context, log(msg), "
            "datetime, dateutil, json, và các biến extra_context theo từng use case.\n"
            "Code BẮT BUỘC phải gán biến `result` ở cuối."
        ),
    )

    def _get_eval_context(self, extra_context=None):
        self.ensure_one()
        ctx = {
            "env": self.env,
            "record": self.env.context.get("active_record"),
            "records": self.env.context.get("active_records"),
            "context": dict(self.env.context),
            "log": lambda msg: _logger.info("[connector.code][%s#%s] %s",
                                             self._name, self.id, msg),
            "datetime": datetime,
            "dateutil": dateutil,
            "json": json,
            "result": None,
        }
        if extra_context:
            ctx.update(extra_context)
        return ctx

    def execute_code(self, extra_context=None, raise_on_error=True):
        self.ensure_one()
        if not self.code:
            raise ValidationError(_("Chưa nhập code cho %s") % self.display_name)

        eval_context = self._get_eval_context(extra_context)
        log_vals = {
            "res_model": self._name,
            "res_id": self.id,
            "code_snapshot": self.code,
            "user_id": self.env.uid,
        }
        try:
            safe_eval(self.code, eval_context, mode="exec", nocopy=True)
            result = eval_context.get("result")
            log_vals.update({"state": "success", "result_summary": str(result)[:2000]})
            return result
        except Exception as e:
            log_vals.update({"state": "error", "error_message": str(e)})
            _logger.exception("Lỗi thực thi code trên %s#%s", self._name, self.id)
            if raise_on_error:
                raise ValidationError(
                    _("Lỗi khi thực thi code:\n%s") % str(e)
                ) from e
            return None
        finally:
            self.env["connector.code.execution.log"].sudo().create(log_vals)
```

### `connector.code.execution.log`

```python
class ConnectorCodeExecutionLog(models.Model):
    _name = "connector.code.execution.log"
    _description = "Audit log mỗi lần execute code"
    _order = "create_date desc"

    res_model = fields.Char(required=True)
    res_id = fields.Integer(required=True)
    user_id = fields.Many2one("res.users")
    code_snapshot = fields.Text()
    state = fields.Selection([("success", "Success"), ("error", "Error")])
    result_summary = fields.Text()
    error_message = fields.Text()
```

### Security group

```xml
<!-- security_groups.xml -->
<record id="group_connector_developer" model="res.groups">
    <field name="name">Connector Developer</field>
    <field name="category_id" ref="base.module_category_hidden"/>
    <field name="implied_ids" eval="[(4, ref('base.group_system'))]"/>
</record>
```

> Field `code` chỉ hiện/sửa được với group này — set qua thuộc tính `groups=` trên field, **không** đủ nếu chỉ ẩn trên view (phải chặn ở field definition để chặn cả write qua API/import).

---

## 4. Model: `connector.model.signature` (Discovery / Schema)

```python
class ConnectorModelSignature(models.Model, ConnectorCodeMixin):
    _name = "connector.model.signature"
    _inherit = ["connector.code.mixin"]
    _description = "Định nghĩa cách discover schema từ 1 JSON path"

    endpoint_id = fields.Many2one("connector.endpoint", ondelete="cascade")
    target_model_name = fields.Char(
        required=True,
        help="vd: x_shopify_order — bắt buộc prefix x_, lowercase, không dấu chấm",
    )
    parent_signature_id = fields.Many2one("connector.model.signature")
    json_path_key = fields.Char(
        help="Path cố định để định danh model con, vd 'lines' hoặc 'lines.taxes' "
             "— KHÔNG đánh số theo thứ tự duyệt để tránh drift khi API đổi order field."
    )
    use_custom_discovery = fields.Boolean()

    @api.constrains("target_model_name")
    def _check_model_name(self):
        for rec in self:
            name = rec.target_model_name or ""
            if not name.startswith("x_"):
                raise ValidationError(_("Model name phải bắt đầu bằng 'x_'"))
            if "." in name:
                raise ValidationError(_("Model name không được chứa dấu chấm"))
            if not re.match(r"^[a-z0-9_]+$", name):
                raise ValidationError(_("Model name chỉ chứa lowercase và underscore"))
```

### Thuật toán Discovery mặc định (khi `use_custom_discovery = False`)

```python
def discover_schema(self, raw_json, path_prefix=""):
    """
    Trả về structure:
    {
        "models": [{"name": "x_..._1", "json_path_key": "lines", "parent": "x_order"}],
        "fields": [{"model": "x_order", "name": "x_price", "type": "text"}],
        "relations": [{"parent": "x_order", "child": "x_..._1", "type": "one2many"}],
    }
    """
    discovered = {"models": [], "fields": [], "relations": []}
    self._walk_json(raw_json, self.target_model_name, discovered)
    return discovered

def _walk_json(self, node, current_model, discovered, path_key=""):
    if not isinstance(node, dict):
        return
    for key, value in node.items():
        field_name = f"x_{key}"
        if isinstance(value, list):
            # Array -> sinh model con, khoá theo json_path_key (không đánh số theo thứ tự)
            child_path_key = f"{path_key}.{key}" if path_key else key
            child_model = self._resolve_or_create_model_name(current_model, child_path_key)
            discovered["models"].append({
                "name": child_model, "json_path_key": child_path_key, "parent": current_model,
            })
            discovered["relations"].append({
                "parent": current_model, "child": child_model, "type": "one2many",
            })
            # Đi sâu vào phần tử đầu tiên (mẫu) để lấy field của model con
            if value and isinstance(value[0], dict):
                self._walk_json(value[0], child_model, discovered, child_path_key)
        elif isinstance(value, dict):
            # Object -> KHÔNG sinh model, chỉ tạo field Text lưu nguyên JSON
            discovered["fields"].append({"model": current_model, "name": field_name, "type": "text"})
        else:
            # Primitive -> luôn Text
            discovered["fields"].append({"model": current_model, "name": field_name, "type": "text"})
```

> `_resolve_or_create_model_name`: tra cứu bảng map `json_path_key -> model_name` đã lưu từ lần discovery trước (để giữ tên ổn định qua nhiều lần chạy); nếu chưa có, sinh tên mới dạng `<root>_<index_thu_tu_sinh>` nhưng **lưu cố định kèm `json_path_key`** để lần sau tra lại đúng model cũ, không sinh mới.

### Khi `use_custom_discovery = True`

```python
def discover_schema(self, raw_json):
    if self.use_custom_discovery:
        return self.execute_code(extra_context={
            "raw_json": raw_json,
            "target_model": self.target_model_name,
        })
    return self._default_discover_schema(raw_json)
```

---

## 5. Service: `connector.schema.builder`

Chịu trách nhiệm **Apply Schema** — chỉ chạy sau khi Discovery xong toàn bộ.

```python
class ConnectorSchemaBuilder(models.AbstractModel):
    _name = "connector.schema.builder"

    def apply_schema(self, discovered):
        """discovered: kết quả gộp từ discover_schema (có thể của nhiều signature)."""
        model_cache = {}
        for m in discovered["models"]:
            model_cache[m["name"]] = self._get_or_create_model(m["name"])

        for f in discovered["fields"]:
            self._get_or_create_field(model_cache[f["model"]], f["name"], f["type"])

        for r in discovered["relations"]:
            self._get_or_create_relation(model_cache[r["parent"]], model_cache[r["child"]], r["type"])

        self._generate_views(model_cache)
        # Registry reload xảy ra ở đây, 1 LẦN duy nhất cho cả batch — không reload
        # riêng lẻ theo từng field như khi tạo rải rác trong lúc import.
        self.env.registry.setup_models(self.env.cr)

    def _get_or_create_model(self, name):
        IrModel = self.env["ir.model"].sudo()
        existing = IrModel.search([("model", "=", name)], limit=1)
        if existing:
            return existing
        model = IrModel.create({
            "name": name, "model": name, "state": "manual",
        })
        # Field payload + external_id mặc định + access rights
        self.env["ir.model.fields"].sudo().create([
            {"model_id": model.id, "name": "x_payload",
             "field_description": "Raw Payload", "ttype": "text"},
            {"model_id": model.id, "name": "x_external_id",
             "field_description": "External ID", "ttype": "char"},
        ])
        self._create_access_rights(model)
        return model

    def _create_access_rights(self, model):
        self.env["ir.model.access"].sudo().create({
            "name": f"access_{model.model}_connector",
            "model_id": model.id,
            "group_id": self.env.ref("connector_base.group_connector_developer").id,
            "perm_read": 1, "perm_write": 1, "perm_create": 1, "perm_unlink": 1,
        })

    def _get_or_create_field(self, model, name, ttype):
        IrField = self.env["ir.model.fields"].sudo()
        existing = IrField.search([("model_id", "=", model.id), ("name", "=", name)], limit=1)
        if existing:
            return existing
        return IrField.create({
            "model_id": model.id, "name": name,
            "field_description": name.replace("x_", "").replace("_", " ").title(),
            "ttype": "text",  # luôn Text theo triết lý raw-first
        })

    def _get_or_create_relation(self, parent_model, child_model, rel_type):
        # Tạo field Many2one trên child trỏ về parent + One2many ngược trên parent
        IrField = self.env["ir.model.fields"].sudo()
        m2o_name = f"x_{parent_model.model}_id"
        if not IrField.search([("model_id", "=", child_model.id), ("name", "=", m2o_name)]):
            IrField.create({
                "model_id": child_model.id, "name": m2o_name,
                "field_description": "Parent", "ttype": "many2one",
                "relation": parent_model.model, "ondelete": "cascade",
            })
        o2m_name = f"x_{child_model.model.split('_')[-1]}_ids"
        if not IrField.search([("model_id", "=", parent_model.id), ("name", "=", o2m_name)]):
            IrField.create({
                "model_id": parent_model.id, "name": o2m_name,
                "field_description": "Lines", "ttype": "one2many",
                "relation": child_model.model, "relation_field": m2o_name,
            })
```

### Sinh View (Form + Tree)

```python
    def _generate_views(self, model_cache):
        for model in model_cache.values():
            self._generate_tree_view(model)
            self._generate_form_view(model)

    def _generate_tree_view(self, model):
        fields_ = self.env["ir.model.fields"].sudo().search([
            ("model_id", "=", model.id), ("ttype", "!=", "one2many"),
        ])
        arch_fields = []
        for i, f in enumerate(fields_):
            optional = "show" if i < 5 else "hide"
            arch_fields.append(f'<field name="{f.name}" optional="{optional}"/>')
        arch = f'<tree>{"".join(arch_fields)}</tree>'
        self._create_or_update_view(model, "tree", arch)

    def _generate_form_view(self, model):
        own_fields = self.env["ir.model.fields"].sudo().search([
            ("model_id", "=", model.id), ("ttype", "not in", ["one2many"]),
        ])
        child_o2m = self.env["ir.model.fields"].sudo().search([
            ("model_id", "=", model.id), ("ttype", "=", "one2many"),
        ])
        group_fields = "".join(f'<field name="{f.name}"/>' for f in own_fields)
        notebook_pages = "".join(
            f'<page string="{f.field_description}"><field name="{f.name}"><tree editable="bottom">'
            f'</tree></field></page>' for f in child_o2m
        )
        arch = f'<form><sheet><group>{group_fields}</group>' \
               f'<notebook>{notebook_pages}</notebook></sheet></form>'
        self._create_or_update_view(model, "form", arch)
```

---

## 6. Service: `connector.import.engine`

```python
class ConnectorImportEngine(models.AbstractModel):
    _name = "connector.import.engine"

    def run_sync(self, endpoint):
        connector = endpoint.connector_id
        cache = {"models": {}, "fields": {}}   # cache trong phạm vi 1 request/run
        missing_log = {}                       # gộp missing field, log 1 lần cuối

        all_records = self._fetch_paginated(endpoint)

        if connector.auto_create_schema and not endpoint._schema_applied:
            discovered = endpoint.root_model_id.discover_schema(all_records[0])
            self.env["connector.schema.builder"].apply_schema(discovered)
            endpoint._schema_applied = True  # đóng băng schema sau lần đầu

        for raw in all_records:
            self._import_record(raw, endpoint.root_model_id, cache, missing_log)

        self._flush_missing_log(missing_log, endpoint)

    def _fetch_paginated(self, endpoint):
        connector = endpoint.connector_id
        results = []
        page = 1
        cursor = None
        while True:
            params = self._build_pagination_params(endpoint, page, cursor)
            resp = connector._request(endpoint.method, endpoint.path, params=params)
            batch = self._extract_target_path(resp, endpoint.target_json_path)
            if not batch:
                break
            results.extend(batch)
            if endpoint.pagination_type == "none":
                break
            if endpoint.pagination_type == "cursor":
                cursor = self._extract_target_path(resp, endpoint.pagination_next_path)
                if not cursor:
                    break
            else:
                if len(batch) < endpoint.pagination_page_size:
                    break
                page += 1
        return results

    def _extract_target_path(self, data, path):
        if not path:
            return data
        node = data
        for key in path.split("."):
            node = node.get(key, {}) if isinstance(node, dict) else None
            if node is None:
                return None
        return node

    def _import_record(self, raw, signature, cache, missing_log, parent_id=None):
        Model = self._get_cached_model(signature.target_model_name, cache)
        vals = {"x_payload": json.dumps(raw)}
        external_id = raw.get("id") or raw.get("Id") or raw.get("ID")
        if external_id is not None:
            vals["x_external_id"] = str(external_id)
        child_batches = {}

        for key, value in raw.items():
            field_name = f"x_{key}"
            field = self._get_cached_field(signature.target_model_name, field_name, cache)
            if field is None:
                missing_log.setdefault(signature.target_model_name, set()).add(field_name)
                continue
            if isinstance(value, list):
                child_batches[key] = value
            else:
                vals[field_name] = json.dumps(value) if isinstance(value, dict) else str(value)

        # Upsert theo external_id thay vì create thẳng
        record = None
        if vals.get("x_external_id"):
            record = Model.sudo().search([("x_external_id", "=", vals["x_external_id"])], limit=1)
        if record:
            record.write(vals)
        else:
            record = Model.sudo().create(vals)

        for key, items in child_batches.items():
            child_signature = signature._get_child_signature(key)
            for item in items:
                self._import_record(item, child_signature, cache, missing_log, parent_id=record.id)

        return record

    def _flush_missing_log(self, missing_log, endpoint):
        for model_name, field_names in missing_log.items():
            self.env["connector.missing.log"].sudo().create({
                "endpoint_id": endpoint.id,
                "model_name": model_name,
                "field_names": ", ".join(sorted(field_names)),
            })
```

> `x_external_id` cần **unique constraint** (SQL `_sql_constraints` hoặc index unique) trên mỗi model runtime để tránh race-condition tạo trùng khi sync song song.

---

## 7. Sequence tổng thể 1 lần sync

```
User bấm "Sync" trên connector.endpoint (hoặc cron trigger)
        │
        ▼
ConnectorImportEngine.run_sync(endpoint)
        │
        ├─► connector._request() [đính auth headers, retry theo config]
        │
        ├─► _fetch_paginated() → list[dict] raw records
        │
        ├─► nếu auto_create_schema và chưa apply lần nào:
        │        ├─► signature.discover_schema(sample_record)
        │        └─► schema_builder.apply_schema(discovered)
        │                  └─► registry.setup_models() [1 lần]
        │
        ├─► loop từng record:
        │        └─► _import_record() [upsert theo external_id, cache field lookup]
        │
        └─► flush missing_log → connector.missing.log [1 record/model, gộp field]
```

---

## 8. Checklist triển khai Phase 1 (thứ tự làm)

1. `connector.config` + auth resolver (bearer trước, basic/oauth2 sau).
2. `connector.endpoint` + `_request()` cơ bản (không pagination).
3. `connector.code.mixin` + `connector.code.execution.log` + security group.
4. `connector.model.signature` + thuật toán discovery mặc định (`_walk_json`).
5. `connector.schema.builder`: tạo model/field trước, quan hệ + view sau.
6. `connector.import.engine`: import phẳng (không nested) trước, thêm nested sau.
7. Upsert theo `x_external_id` — làm cùng lúc với bước 6, không để sau.
8. `ir.model.access` tự động khi tạo model — bắt buộc trong bước 5, không để riêng.
9. Pagination (page → offset → cursor).
10. Missing log gộp + Auto Schema OFF flow.
11. Cron scheduler cho sync định kỳ.

Mỗi bước nên có unit test riêng với JSON mẫu **cố ý làm bẩn** (field null, key đổi thứ tự, array rỗng, object lồng 3 cấp) để bám sát khuyến nghị "spike trước khi build full" ở tài liệu thiết kế.